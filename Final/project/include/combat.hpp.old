#ifndef COMBAT_HPP
#define COMBAT_HPP

#include <iostream>
#include <cstdlib>
#include "Enemy.hpp"
//#include "Player.hpp"
//#include "Display.hpp"
#include "Unit.hpp"
#include "Weapon.hpp"
#include "game-utility.hpp"

class Combat {
protected:
    int kills;
    std::string action;

  public:
    //Default Constructor
    Combat();
    //Overloaded Constructor
    Combat(int action_);
    void attack_other(Player& p, Enemy& e);
    void on_attack(int damage);
    bool is_dead();
    void on_death();
    void combat_phase();

    //Getters
    int get_kills();
    std::string get_action();
    //setters
    void set_kills(int k);
    void set_action(std::string a);

};

#endif // COMBAT_HPP
