#ifndef DISPLAY_HPP
#define DISPLAY_HPP

#include <ncurses.h>
#include "Unit.hpp"
#include <vector>
#include <string>

class Display {
private:
	//Unit Player;
    int yshift;
    int selecter;
    Unit unit;

public:
  //Default Constructor
  Display();
  //Overloaded Constructor
  Display(int yshift_, int selecter_, std::string name_, int health_, int mana_, int attack_, int defense_);

    void drawGame(int yMax, int xMax);
    void updateDialogue(std::string dialogue);
    void updateCombat(std::string name, int damage);
    void updateDeath(std::string name);
    void updateAvatarHUD(Display player);
    void updateEnemyHUD(Display enemy);
    void updateOptions();
    //void drawMenu();
    //Getters
    int get_yshift();
    int get_selecter();
    Unit get_unit();

    //Setters
    void set_yshift(int y);
    void set_selecter(int s);
    void set_unit(Unit unit_);
};

#endif /* DISPLAY_HPP */
